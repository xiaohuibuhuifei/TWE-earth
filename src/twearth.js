import { initEarth, flyTo } from "./core";
import Entity from "./entity";
import { author, version } from "../package.json";

export default {
  author: author,
  version: version,
  initEarth,
  flyTo,
  Entity,
};
